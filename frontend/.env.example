# Frontend Environment Configuration

# Base path for the application
# Examples:
#   VITE_BASE_PATH=/demo    → App runs at http://localhost/demo
#   VITE_BASE_PATH=/         → App runs at http://localhost/ (root)
#   VITE_BASE_PATH=/admin   → App runs at http://localhost/admin
VITE_BASE_PATH=/demo

# API configuration
# The backend API domain (usually same origin in production)
VITE_API_DOMAIN=http://localhost/api

# Note: Vite only exposes env vars prefixed with VITE_ to the client
# These can be accessed in the app using import.meta.env.VITE_*
